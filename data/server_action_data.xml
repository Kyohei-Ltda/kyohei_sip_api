<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Invoices -->
    <record id="get_sip_token_server_action" model="ir.actions.server">
        <field name="name">Get sip token</field>
        <field name="model_name">res.company</field>
        <field name="model_id" ref="base.model_res_company"/>
        <field name="binding_model_id" ref="base.model_res_company"/>
        <field name="state">code</field>
        <field name="code">
for record in records:
    server_response = record._get_sip_auth_token()
    action = {
        "type": "ir.actions.client",
        "tag": "display_notification",
        "params": {
        "title": "Obtención token SIP",
        "message": server_response,
        "sticky": False,
            }
        }
        </field>
    </record>

</odoo>