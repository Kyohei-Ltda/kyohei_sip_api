<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_provider_form" model="ir.ui.view">
        <field name="name">SIP Provider Form</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="provider_credentials" position='inside'>
                <group invisible="code != 'sip'">
                    <field name="sip_merchant_identifier"
                           string="Merchant Identifier"
                           required="code == 'sip' and state != 'disabled'"/>
                    <field name="sip_access_code"
                           string="Access Code"
                           required="code == 'sip' and state != 'disabled'"
                           password="True"/>
                    <field name="sip_sha_request"
                           string="SHA Request Phrase"
                           required="code == 'sip' and state != 'disabled'"
                           password="True"/>
                    <field name="sip_sha_response"
                           string="SHA Response Phrase"
                           required="code == 'sip' and state != 'disabled'"
                           password="True"/>
                </group>
            </group>
        </field>
    </record>

</odoo>
